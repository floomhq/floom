# Sync Wave 2026-06-06

Executed from `/tmp/sync-wave-brief.md` on 2026-06-06/2026-06-07 CEST.

## Final Pins

- OSS `floomhq/workeros` main before this report: `efb480fe888d5b3f22e99c5ba5ab0a600fe6bb8c`
- Cloud `floomhq/workeros-cloud` main after sync: `502365578a3b8b82ceb81be0313ef7255b44f028`
- Cloud `engine` submodule pin: `efb480fe888d5b3f22e99c5ba5ab0a600fe6bb8c`
- Source pin match: verified. Cloud `engine` equals OSS main.

## Phase 1: PR Decisions

| PR | Decision | Merge SHA / Status | Reason |
|---|---:|---|---|
| #479 | GO | `1bb5085` | Docs/design storage note only. |
| #462 | GO | `9b3e654` | Docs/issues/design only. |
| #460 | GO | `f3c384e` | Slack install scopes from env; no regression found in diff review. |
| #482 | GO | `05ef2ed` | Worker create reliability fix; live provider path verified post-deploy. |
| #483 | GO | `c5859ec` | Worker failure alerting; tests passed in backend batch. |
| #486 | GO | `63e16fd` | Protected worker PUT fork and CLI auth behavior; tests passed. |
| #484 | GO | `6978e41` | Worker management bugs; conflict resolved with #486 protected-stock PUT behavior and workspace guards. |
| #481 | GO | `8946506` | Base persona editor; targeted regression passed. |
| #477 | GO | `d36c72c` | Connections progress UI; web gates passed. |
| #478 | GO | `f146503` | Developer tab; web gates passed. |
| #480 | GO | `7bcb2b2` | Prompt tool detector; web gates passed. |
| #476 | GO | `4759b41` | v6 share pages merged as instructed. Approval-page tall-card rework remains a Federico layout follow-up. |
| #485 | GO | `7b1b8ee` | Workeros branding/breadcrumbs; conflicts resolved by keeping #476 v6 share components. |
| #487 | GO | `80c1c3b` | Emily chat persistence; web gates passed after hook-order fix. |
| #408 | HOLD | Open draft | Preview-only worker-detail experiment; draft and conflicting. |

New OSS PR #489 appeared after the requested wave and was not part of this brief.

## Phase 2: OSS Merge Gates

Merge work was performed in clean worktree `/tmp/workeros-sync-wave`.

- Final OSS main push: `efb480fe888d5b3f22e99c5ba5ab0a600fe6bb8c`
- Additional integration/fix commits:
  - `e418485` fixed #476 standalone share hook ordering.
  - `e913a41` let `/auth/me` reach the multi-member provider after concurrent main integration.
- Gates run:
  - `npm --prefix apps/web run lint`: passed with warnings.
  - `npm --prefix apps/web run build`: passed.
  - Targeted API pytest batches: `77 passed`, `21 passed`, `1 passed`, and final multi-member/base-persona batch `30 passed`.
  - `npm test` in `apps/mcp`: `35 passed`.

## Phase 3: OSS Deploy Verification

Deploy command: `/opt/workeros-api-deploy/ops/deploy-api.sh`.

The deploy script restarted `workeros-api` and passed its built-in health/key endpoint checks. It exited `1` only because `/root/workeros/ops/smoke-routes.sh` is missing; manual verification continued.

Manual production checks:

- `systemctl is-active workeros-api`: active.
- Local `http://127.0.0.1:8011/healthz`: `200 {"status":"ok"}`.
- Public `https://workers-api.floom.dev/healthz`: `200`.
- Public `https://workers-api.floom.dev/health`: `200`, db/disk/e2b/openai/composio OK.
- Worker create/get/delete live roundtrip: passed.
- Emily `POST /chat` SSE smoke: `200`, `chat.meta`, `text`, and `finish` events in 3.77s.
- `https://workers.floom.dev/`: `200` effective `/login`; loaded Workeros sign-in page, screenshot evidence at `/tmp/sync-wave-evidence/screenshots/workers-floom-login-loaded.png`.

Live #482 smoke:

- `/workers/new/from-prompt` created `sync-wave-live-smoke-1780782087` via the real provider path.
- Authoring run completed in 41.69s.
- Generated worker run completed in 6.99s with output `{"result":"cba"}`.
- Cleanup delete returned `204`.

## Phase 4: Cloud Sync and Deploy

Cloud work was performed in clean worktree `/tmp/workeros-cloud-sync-wave` because `/root/workeros-cloud` had unrelated dirty work.

Completed source work:

- Engine bump commit: `136040c` (`17f49c871ee031d52ccfc88ee6d71d8bcff7177f -> efb480fe888d5b3f22e99c5ba5ab0a600fe6bb8c`).
- Cloud PR #109 merged: `e622b00`.
- Overlay compatibility commit: `5023655`.
  - Preserved PR #109 dropdown highlight fix as `web/overlay/components/ui/dropdown-menu.tsx`.
  - Added `api.conversations.list/get` to the Cloud API overlay for synced Emily chat persistence.
  - Restored `web/overlay/lib/types.ts` as an exact engine-types copy so overlay source type-checks.
- Cloud main pushed: `502365578a3b8b82ceb81be0313ef7255b44f028`.

Cloud local gates:

- `npm --prefix web run sync`: passed.
- `npm --prefix web run check-drift`: passed, zero drift.
- `npm --prefix web run lint`: passed with 46 warnings and 0 errors.
- `npm --prefix web run build`: passed.
- Vercel production prebuild from clean repo root, linked to dashboard project `prj_sAQxAnqsBmNlfDcLvPwzt6LyS1S5`: passed and produced `/app` dashboard routes.

Cloud deploy status:

- GitHub Actions for Cloud SHA `5023655` failed before any steps ran. Job records had empty `runner_name` and empty `steps`, so no code/build failure was available in logs.
- Manual Vercel prebuilt deploy from the clean worktree failed during file upload: Vercel API `https://api.vercel.com/v2/files` returned an `Internal Server Error` non-JSON response and the upload aborted before a deployment URL was issued.
- `workeros-cloud-api-autodeploy.service` is failing because its deployment checkout cannot fast-forward (`fatal: Not possible to fast-forward, aborting`).

Live Cloud route evidence gathered after the deploy blocker:

- `https://workeros.floom.dev/app`: `200` after redirect to `/app/login?next=%2Fapp`.
- `https://workeros.floom.dev/app/assistant`: `200` after redirect to `/app/login?next=%2Fapp%2Fassistant`.
- Current live dashboard deployment id in HTML: `dpl_HuYJyiWvuHSydxstGvcCGG1U3Grs`.
- `https://workeros-api.floom.dev/healthz`: `200 {"status":"ok","deploy":"cloud"}`.
- `https://workeros-api.floom.dev/health`: `200`, db/disk/e2b/openai/composio OK.
- `https://workeros-api.floom.dev/version` and `/api/version`: `404`, so no live API version endpoint verified the engine SHA.

Cloud production deploy verification is incomplete because both CI/Vercel deployment paths and the Cloud API autodeploy path are blocked as described above. Source-level engine pin verification is complete.

## Held / Follow-Up Items

- #408 remains held: draft, preview-only, and not intended for this shipping wave.
- #476 approval page keeps the known tall-card layout. Approval-page rework is tracked as a Federico layout decision, not silently changed in this wave.
- Cloud deployment requires unblocking:
  - GitHub runner/job startup failure for `floomhq/workeros-cloud`.
  - Vercel file upload API failure for the dashboard project.
  - `workeros-cloud-api-autodeploy.service` fast-forward divergence in the deployed API checkout.

