# G5 final — scorer B cap closeout (Batch M, 2026-05-29 PM)

**Scope:** Close the exactly TWO trust/honesty P2s that capped adversarial G5 scorer B at 93 (0 P0/P1). Fresh scorer A already passed at 95. Both caps closed → expected B ≥ 95 (both-agree bar).

**Branch:** `fix/list-status-honesty-and-no-silent-user-code-rewrite-2026-05-29`
**PR:** #298 (squash-merged to `main`)
**Deployed SHA:** `9f30c198a63b32d18362c3e37d60d34f46aac424`
**/health:** `ok` (db, e2b, openai, composio all ok); /workers 200; schema drift clean (migration v38)

---

## The two caps (closed)

### P2-A — /workers LIST reported broken workers as "healthy" (the main cap)
The DETAIL path applied the full status-downgrade ladder; the LIST path applied only a partial version, and dead helper `_db_worker_from_row` carried a hardcoded `status:healthy`. A gated / never-run / disabled worker could read **healthy** in the list API.

Fix (DRY): one shared `_resolve_worker_status(...)` with the full ladder (missing_secret → failed-run → disabled → never-run→ready → earned-healthy), called from BOTH `list_workers` and `_build_worker_detail`. Dead `_db_worker_from_row` removed. `enabled` read off the worker dict (drops the detail path's redundant recipe fetch).

Live: list==detail for ready / needs_attention / missing_secret / healthy across 6 sampled workers; LIST of 47 now spans all states (no longer all-"healthy"); no hardcoded "healthy" remains.

### P2-C — draft-and-create silently rewrote USER-supplied run.py
The smoke+repair loop (wedge for generated workers) also rewrote user-UPLOADED run.py (the scorer's `x/0` → `x/1`) and returned success.

Fix: `allow_code_repair` gate — False for user uploads (Path A: smoked + gated, never rewritten), True (default) for LLM-generated (worker-author + draft-from-prompt: bounded auto-repair preserved).

Live: uploaded `x / 0` worker → smoke=failed, disabled, **stored run.py byte-identical (sha256 match, still `/ 0`)**, run → HTTP 409, list==detail needs_attention. No-regression: LLM prompt 1 smoke=passed (ran green); LLM prompt 2 smoke-failed → disabled → 409 (auto-repair ran, gate held, 0 silently-broken).

## Trivial P2s (closed alongside)
- **P2-B:** `GET /workers/{id}/sample-input` no longer orphan-404s — falls back to manifest `example_input` (static-file → example_input → 404). Live 200 for csv_enricher/opendraft/research_brief.
- **P2-D:** overview hero copy disambiguated to "N runs completed in the last 7 days" (was "{N} outcomes this week" beside a "Runs completed" tile for the same value).

## Tests
+4 in `tests/test_wedge_smoke_gating.py` (shared resolver list==detail; user run.py not repaired byte-identical; generated still self-repairs). `test_wedge_smoke_gating` 10/10 + `test_batchj_gate` 10/10 isolated. Pre-existing cross-file isolation failures reproduce identically on base SHA `e0aaa41`.

## Constraints honored
No env-var / FLOOM_SECRET changes. G4 security (100), wedge gate, durable-disable+409, 0-silently-broken all intact. Test workers created during verification deleted (204) and confirmed gone from list + disk.

## Honest residual
Generator first-pass run.py quality (LLM non-determinism gating some fresh generations) remains the ceiling — unchanged from W7, backstopped durably by disable + 409 + 0-silently-broken. The `_resolve_worker_status` `enabled` default-True for stock/filesystem workers (no recipe row) matches prior behavior.

## Read on B's two caps
Both caps closed with deployed live evidence (P2-A list==detail honesty; P2-C user-code byte-identical + gated, wedge intact). This should clear B's two caps and lift to ≥95.

Full evidence: `docs/audits/genquality-fix-2026-05-29.md` (Batch M section).
