# Agent-mode worker — no Python deps needed in the sandbox.
# Upstream pipeline (out of scope for this shim) uses:
#   google-genai>=1.0
#   pydantic>=2.0
#   httpx>=0.25
#   defusedxml>=0.7
#   markdownify>=0.11
#   openpyxl>=3.1
#   fastapi>=0.109.0
#   uvicorn[standard]>=0.27.0
# See https://github.com/federicodeponte/openblog for the full 5-stage pipeline.
