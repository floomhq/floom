-r ../../engine/apps/api/requirements.txt
email-validator==2.3.0
psycopg[binary]==3.2.9
resend==2.30.1
supabase==2.31.0

# Vertex AI (Gemini) auth for litellm: the `vertex_ai/*` provider path imports
# google.auth to mint tokens (incl. the AWS->GCP Workload Identity Federation
# external_account flow). The engine requirements don't ship it, so litellm
# raises ModuleNotFoundError: google.auth on any Vertex call. TEMPORARY here to
# unblock prod; remove once floomhq/floom adds it to engine requirements and
# the submodule is bumped. google-cloud-aiplatform is NOT needed (REST + token).
google-auth>=2.49,<3

# CI: pin to fix collection error on self-hosted runner (0.23.3 Package.obj bug; option added 0.24.0)
pytest-asyncio==0.24.0
